# 手动部署到生产环境
# 配置说明：docs/DEPLOY-GITHUB-ACTIONS.md
# 注意：此文件已迁移到仓库根目录的 .github/workflows/deploy-prod.yml
# 此文件保留作为参考，实际使用的是根目录的版本

name: Deploy to ECS (Deprecated)

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "要部署的分支（服务器上将 git checkout / pull 此分支）"
        required: true
        default: "main"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: ⚠️ 提示
        run: |
          echo "此工作流已弃用，请使用仓库根目录的 .github/workflows/deploy-prod.yml"
          echo "访问：https://github.com/lvzhaobo/ira/actions"
