# 14 — 需求追踪矩阵

| 属性 | 内容 |
|------|------|
| **模块编号** | M2 |
| **模块名称** | Glue Coding · 多源数据（Wind / 新浪等） |
| **文档包版本** | v1.2 |
| **技术栈** | 前端 React · 后端 Flask · 代码 GitHub |
| **开发方式** | CoPaw + 本模块 Spec |
| **模块侧重** | 多源行情与资讯的采集、清洗、调度与可观测；强调数据管道与失败降级。 |

---

## 1. 与总表对应

总表 **05**（追踪矩阵）。

## 2. 追踪矩阵（P0）

| REQ ID | US ID | 规格 | 主要 `09` 端点 | 测试 TC-ID |
|--------|-------|------|----------------|------------|
| REQ-M2-001 | US-M2-001 | `06`；`09` §3.2～3.6 | `GET /ingest/sources`、`POST /ingest/jobs`、`GET .../jobs`、`GET .../jobs/{id}` | M2-I-JOB-01、M2-E2E-001 |
| REQ-M2-002 | US-M2-002 | `07` §3；`09` §1.1 | 错误响应与任务 `errors` | M2-I-FAIL-01 |
| REQ-M2-003 | US-M2-003 | `09` §1.2 `M2_JOB_ALREADY_RUNNING` | `POST /ingest/jobs` 并发策略 | （补充 TC：M2-I-CONC-01） |

## 3. 跨模块

| 消费方 | 说明 |
|--------|------|
| **M1** | `research_messages` 增量；变更 `10` §5 须同步 M1 `14` |
| **M3** | 清洗文本导出 P1，矩阵可后置 |

## 4. 变更流程

改 `09` 任务体 / `stats` 键 → 更新 **`13` 断言** + **本表** + 运维看板（若有）。

## 5. PR 影响勾选

```
- [ ] 迁移 / 新源类型
- [ ] 写 M1 映射变更
- [ ] 回归：`13` §4
```

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-04-04 | 从原 `11` 拆分 |
| v1.2 | 2026-04-04 | P0 映射与流程 |
