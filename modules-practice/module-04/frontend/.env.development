# 使用 package.json 的 proxy 时，allowedHosts 会取 urls.lanUrlForConfig；
# 在部分环境下为 undefined，webpack-dev-server 会报错：
# allowedHosts[0] should be a non-empty string
# 官方说明见 advanced-configuration（DANGEROUSLY_DISABLE_HOST_CHECK）
DANGEROUSLY_DISABLE_HOST_CHECK=true
